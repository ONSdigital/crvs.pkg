% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_asfr_usual_res.R
\name{calc_age_spec_fert_rate_m}
\alias{calc_age_spec_fert_rate_m}
\title{Calculate Age-Specific Fertility Rates for Mothers}
\usage{
calc_age_spec_fert_rate_m(
  data,
  pop,
  fcts,
  pop_sex = "Female",
  fert_range = 15:44,
  by_pop = 1000,
  digits = 1
)
}
\arguments{
\item{data}{A data frame containing birth records. Must include:
\itemize{
\item \code{dobyr} — year of birth
\item \code{age_grp_m} — mother's age group
\item \code{usual_residence} — geographic area of residence
\item \code{live_or_still} — indicator of live or still birth
}}

\item{pop}{A data frame containing population counts. Must include:
\itemize{
\item \code{year} — year of population estimate
\item \code{geog} — geography
\item \code{age} — single-year age
\item \code{population} — count of women
}}

\item{fcts}{A list of factor level definitions used by the \code{group_age()}
function (e.g., mother age group labels). Typically contains
\code{birth_fcts$mother_age_group}.}

\item{by_pop}{Numeric. Population multiplier (e.g., 1000 for rate per 1,000).
Defaults to \code{1000}.}

\item{digits}{Numeric. Number of decimal places to round the result to.
Defaults to \code{2}.}
}
\value{
A tibble containing:
\itemize{
\item \code{dobyr} — year
\item \code{age_grp_m} — mother’s age group (including "All ages")
\item \code{usual_residence} — geography (including "Country")
\item \code{age_specific_fert_rate} — ASFR per 1,000 women
}

The returned table is sorted by decreasing year, then age group, then
geography.
}
\description{
This function computes age-specific fertility rates (ASFR) for mothers by
combining a dataset of birth records with population denominators grouped
into 5-year age bands. It produces rates per 1,000 women for each
age group, geography, and year.
}
\details{
The function performs the following steps:
\enumerate{
\item Filters the population to fertile ages and groups into 5-year age bands.
\item Adds an "All ages" population denominator for each year–geography.
\item Harmonises birth data age groups (e.g., "Under 15" and "15 to 19" →
"Under 20").
\item Summarises birth counts and generates country-level aggregates.
\item Adds an "All ages" birth numerator for each geography–year.
\item Joins births with population denominators.
\item Computes age-specific fertility rates per 1,000 women.
The numerator is the number of live births to mothers in each
age group, geography, and year.
}

The denominator is the female population in the same age group,
geography, and year.

Age groups are aligned between the birth data and population data by
collapsing \code{"Under 15"} and \code{"15 to 19"} into \code{"Under 20"}.

The fertility rate is calculated as:

\deqn{ASFR = (births / population) * 1000}

Rows where \code{usual_residence == "Not stated"} are dropped.
}

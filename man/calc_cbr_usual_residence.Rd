% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_cbr_usual_res.R
\name{calc_cbr_usual_residence}
\alias{calc_cbr_usual_residence}
\title{Calculate crude birth rate by usual residence}
\usage{
calc_cbr_usual_residence(data, pop, fcts, by_pop = 1000, digits = 2)
}
\arguments{
\item{data}{A data frame containing birth registration records. Must include
variables \code{live_or_still}, \code{dobyr}, and \code{usual_residence}.}

\item{pop}{A data frame containing population denominators. Must include
variables \code{year}, \code{geog}, and \code{population}.}

\item{by_pop}{Numeric. The population base for the crude birth rate
(e.g. 1000 for rates per 1,000 population). Default is \code{1000}.}

\item{digits}{Integer. Number of decimal places to round the crude birth rate
to. Default is \code{2}.}
}
\value{
A tibble with one row per year and usual residence, containing:
\itemize{
\item \code{dobyr}: Year of birth
\item \code{usual_residence}: Area of usual residence
\item \code{crude_birth_rate}: Crude birth rate per \code{by_pop} population
}
}
\description{
Calculates the crude birth rate (CBR) by year of birth and usual residence
using live birth registration data and population denominators.
The rate is expressed per a specified population size (default 1,000).
}
\details{
The function:
\itemize{
\item Filters the registration data to live births only.
\item Counts births by year of birth and usual residence.
\item Creates a country-level total for each year  ("Not stated" in usual
residence is counted in country-level but not presented in breakdown).
\item Joins population denominators by year and geography.
\item Calculates crude birth rates using \code{calc_general_rate()}.
\item Excludes records with "Not stated" usual residence.
}

The crude birth rate is calculated as:
\deqn{(Number\ of\ live\ births / Population) \times Population\ multiplier}
}
\seealso{
\code{\link{calc_general_rate}}, \code{\link{group_by_summarise_count}},
\code{\link{group_by_summarise_sum}}
}

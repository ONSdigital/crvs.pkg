% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates.R
\name{calc_general_rate}
\alias{calc_general_rate}
\title{Calculate a General Rate per Population Unit}
\usage{
calc_general_rate(
  data,
  rate_name,
  numerator,
  denominator,
  by_pop = 1000,
  digits = 2
)
}
\arguments{
\item{data}{A data frame containing the variables used in the calculation.}

\item{rate_name}{Name of the new rate column to create, supplied quoted.}

\item{numerator}{Column containing numerator counts, supplied quoted.}

\item{denominator}{Column containing population denominators,
supplied quoted.}

\item{by_pop}{Numeric. Population multiplier (e.g., 1000 for rate per 1,000).
Defaults to \code{1000}.}

\item{digits}{Numeric. Number of decimal places to round the result to.
Defaults to \code{2}.}
}
\value{
A data frame identical to \code{data} but with an additional column
named after \code{rate_name} argument containing the computed rate.
}
\description{
This function computes a general rate (e.g., crude birth rate, crude death
rate, fertility rate) by dividing a numerator (e.g., count) by a denominator
(e.g., population) and scaling by a population unit (default 1,000).
}
\details{
It supports tidy-evaluation so that output column names and input columns
can be supplied unquoted.
}
